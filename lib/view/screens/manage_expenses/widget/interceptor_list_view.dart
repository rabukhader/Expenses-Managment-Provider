import 'package:flutter/material.dart';

class InListOfExpenses extends StatelessWidget {
  const InListOfExpenses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}